@page
@model FaqModel
@{
    ViewData["Title"] = "FAQ";
}
<h1>@ViewData["Title"]</h1>

@await Html.PartialAsync("_FaqNode", Model.Root)

@if (Model.HtmlContent != null)
{
    <hr />
    <div>@Html.Raw(Model.HtmlContent)</div>
}