там была проблемка что бот не понимает что я ему говорю, неправильно интерпретирует.
сейчас я решаю проблему free-form language понимания. это главное.

и одновременно хочу решить проблему ригидной структуры. не хочу переписывать вьюхи.

но тут такие сложные данные что как будто бы даже бизнес-логика какая-то нужна.
на что будет опираться валидация?

по идее архитектор может писать валидационный питон код тоже.
типа функции.
на данных могут выполняться (sql) или python.
По идее оно должно заработать.
и миграции писать тоже будет.

по поводу интерпретации - удобно тыкнуть на сущность или несколько и сказать словами.
такие сообщения-действия уже не реинтерпретируешь.
но когда их наберется много, их надо как-то мигрировать правильно.
выполнимая ли это задача? теоретически - да.
но нужен rich диалог с архитектором. но почему бы нет, уж чаты опенсорсные есть точно.

но вот если что-то запоролось то перенакатить их уже не получится, возможно.
например если ошибка в действии каком-то. хм.

а главное я хочу мочь фиксить баги по жизни приложения.
история применения действий? но действия тоже могут после изменения рождать другие сущности, новые, или не рождать старые.
как реинтерпретировать действие тогда?

и мне нужны агенты, похоже. чёрт.
нерешаемая проблема?

иногда действия пользователя касаются сущностей которые были в прошлой версии, а в новой версии их вообще нет, например они заменены колонками или комбинациями других сущностей. например связь многие-ко-многим с аттрибутами связи. пользователь что-нибудь с ней делает, потому что он её видит на экране. а потом мы мигрируем схему и такой связи вообще не предусмотрено, она уже заменилась чем-то совсем структурно другим, но intent пользователя надо понять и переделать в новую модель если мы хотим сделать re-run свободно. re-run сквозь миграции. или как? понимаешь? тут есть варианты, и хороших кажется нет.

------

тут есть несколько подходов, надо подумать покрывают ли они все сценарии.

## подход 1

1. идут идут ивенты
2. идёт миграция. если что-то потеряется важное, юзер видит по превью, то он просит провести несколько промежуточных миграций чтобы выглядело просто и важное не потерялось.
3. и миграцию, и то что до неё можно перезапустить реинтрепретировать. и проверить что ничего не ломается.

проблема: подменять схему до миграции - уже не получится - миграция сломается.
но зачем это может быть нужно?
а если я пойму что всё не так. хочу начать заново, просто создать новую модель, и сказать влей всё. и не париться с перепроверкой миграций и потерянными данными.

создаю новый мир, он реинтерпретирует старую базу в себя источником.
это по сути и есть миграция.

так зачем подменять схему до миграции?
ну если я пойму "там когда-то была ошибка".
а почему её сейчас уже не исправить? грязные данные?

да потому что блин я не могу данные сначала провести. а часто буду хотеть.
изменить схему и посмотреть что получается. заново.
или какие-то действия не сделать одним сообщением, они делаются многоступенчато.

то есть проблема тут в том что старые сообщения, сама их суть, может иметь прямой смысл только в старой схеме, а в новой схеме оно может быть непонятно совсем. неинтерпретируемо.
и тогда если делать перепрогон, то надо source + new cookbook + interpretation hints v2->vlatest.
но они и могут опираться на данные, иметь ссылки, который вообще vlatest в глаза не видывала.
то есть надо + interpretations v2 + v2 db for lookups.
какая-то жесть в общем случае.

получается миграция какая-то суперсложная, намного проще типа начать заново.
но исходные сообщения некоторые неинтерпретируемы если заново, и надо как-то отличать до сих пор интерпретируемые от тех что мы выкидываем, но выкидывать тоже нельзя, потеряется важный стейт. :(

## подход 2

делать миграцию из данных, а не из исходных сообщений.
но это не прикольно, потому что старые сообщения - по ним не прогнать уже интерпретатор. херово.

## подход 3

все сущности из старой схемы, которые не существуют в новой схеме - по ним запускать нейронку и она будет генерить новые сообщения про них. она данные положит в текст или в json. а в новой схеме интерпретатор этот json интерпретирует.

то есть есть слой напрямую указанного пользователем, или модифицирования того, что существует в новой схеме.
он как бы всегда останется более-менее, хотя блять новая схема.. она может заставить человека говорить по-другому :((( ыыы
ну ладно. а есть сущности которые не восстановить без знания, можно их вытаскивать для реинтерпретации, что ли.
блин, это так сложно. так сложно. чёрт.

# хочу

я хочу чтобы было идеально и данные не терялись совсем, и я всегда мог запустить всё-таки систему исторически с нуля, где только сообщения юзера это источник. и миграционный ИИ автоматически и без ошибок генерил всё что нужно чтобы поддержать этот процесс. так можно?

очень сложно.

# а что если такое решение

## мысли

Телеграм источник или заметки -> схема понимающая. 

То что я билжу дальше, не совсем управление из телеграм -> это планирование - тут возможно другие требования. Тут replay не работает. 

Тут работают миграции и не event sourcing совсем.

Мне бы вообще репозиторий и коммиты и ветки, это было б идеально. С моими данными и схемой. 

Значит два отдельных слоя. Или сервиса. 
Действия с актуальными деньгами - тут схема норм, replay норм.

А планирование - тут хер воспроизведешь, но это и не нужно. Миграция из данных в данные норм сойдёт. Да, просто sql миграция и latest state. Но и репозиторий. Да.

## подробнее

лучше попробую разделить на два.

смотри, есть стрим-источники, типа телеграм, там где ИИ особо даже не разговаривает с человеком или минимально. оно интерпретируется в схему. оно уже более-менее даже есть немножко. @intro-4-ai-poc.md 

это можно юзать для расходов и инвентаризаций. и это вообще может быть универсальный интерпретатор с миграциями вблизи близких к данным. если природа сообщений такая, что там используются какие-то сущности, то никуда от этого особо не уйдешь.

а есть второе, это планировщик. планированием в телеграме не займешься, там много репортов и много операций с одними и теми же сущностями, schema-specific. миграции тоже нужны, автоматические, но об event sourcing и думать нечего, никакие сообщения не интерпретируемы в новых схемах, и вообще исходные сообщения и хранить особо не надо. это вполне база данных с гибкой схемой которая хранит только свой стейт без ивент сорсинга. (в этом стейте бывает что если история где она важна, но только где она важна по бизнесу потому что является частью отчётов).

и в планировщике миграции должны уметь мигрировать данные в данные, без всяких исходных сообщений юзера. с превью и всем таким. и очень хочу чтобы данные желательно были в yaml, ну потому что их немного вообще-то. и удобно смотреть диффы. и бд это private git. там же и схемы и миграции. и можно сделать public git с примерами.

и в планировщик будет подключаться первый сервис. с гибкой схемой. через api например. и если схема мигрируется, естественно в планирощике я тоже настрою миграцию, вернее переподключу маппинги, как-то так. данные первого сервиса второй в себе хранить не будет.

тогда я свободен менять интерпретации, схемы, тут и там, но по-разному, и не объединяя их в одну архитектуру, я избавляюсь от проблем, где первый мешает второму, а второй первому.

правда и первый и второй - это будут УНИВЕРСАЛЬНЫЕ сервисы, вообще никак не заточенные и не привязанные к финансам или бюджетам. я ведь смогу запустить их на любых любых данных же.